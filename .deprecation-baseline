# Diagnostics emitted by src/ that are accepted for now.
# Each needs a breaking change to fix, so they are held for the next major version.
# Regenerate with: php bin/check-deprecations.php --update-baseline

